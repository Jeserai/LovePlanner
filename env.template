# 🔧 LovePlanner 环境变量配置模板
# 复制此文件为 .env.local 并填入实际值

# ==========================================
# Supabase 数据库配置
# ==========================================
# 获取方式：Supabase Dashboard → Settings → API

# Supabase项目URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase匿名公钥 (anon public key)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_public_key_here

# ==========================================
# 应用配置 (可选)
# ==========================================

# 开发环境标识
NODE_ENV=development

# 应用调试模式 (可选)
# NEXT_PUBLIC_DEBUG=true

# 默认主题 (可选)
# NEXT_PUBLIC_DEFAULT_THEME=modern

# ==========================================
# 高级配置 (通常不需要修改)
# ==========================================

# 自定义端口 (可选)
# PORT=3000

# 自定义主机 (可选，用于局域网访问)
# HOST=0.0.0.0

# ==========================================
# 安全提醒
# ==========================================
# ⚠️  重要：
# 1. 不要将 .env.local 文件提交到Git仓库
# 2. 确保 .env.local 在 .gitignore 中
# 3. 生产环境使用不同的密钥
# 4. 定期更换敏感信息
